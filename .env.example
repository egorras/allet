# Allet — example environment. No secrets belong in this file or in the repo.
# v0 is a UI skeleton: it talks to no server, no ticket site, no Telegram, no AI,
# no map provider (ALLET_PLAN.md §8), so nothing here is required to run it.

# Title shown in the browser tab. Optional.
# VITE_APP_TITLE=Allet
